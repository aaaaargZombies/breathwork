module Page.Settings exposing (..)

import Html exposing (Html, a, div, h1, p, text)
import Html.Attributes exposing (href)
import Shared exposing (Model, Msg)


view : Model -> Html Msg
view model =
    div []
        [ h1 [] [ text "Settings [cCc]]" ]
        ]
